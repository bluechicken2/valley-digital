# Deprecated

These standalone analysis engine files are superseded by `xray_engine.py`
which contains the unified `AnalysisEngine` class.
Do not run these directly — use `xray_engine.py --analyze` instead.
